from . import whapi_project_ai
# from . import whapi_aggregates

# def post_init_hook(cr, registry):
    # # call helper
    # try:
        # from .whapi_aggregates import _init_hook
        # _init_hook(cr, registry)
    # except Exception:
        # import logging
        # logging.getLogger(__name__).exception("Failed to run whapi aggregates init hook")